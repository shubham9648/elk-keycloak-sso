# Project namespace (defaults to the current folder name if not set)
COMPOSE_PROJECT_NAME=elastic

# Password for the 'elastic' user (at least 6 characters)
ELASTIC_PASSWORD=changeme

# Password for the 'kibana_system' user (at least 6 characters)
KIBANA_PASSWORD=8vldyW8@

CLIENT_SECRET=zq1FkC2J582Vf1HkWxpS7uIXK08FbR7k
# Version of Elastic products
STACK_VERSION=8.12.1

# Set the cluster name
CLUSTER_NAME=docker-cluster

# Set to 'basic' or 'trial' to automatically start the 30-day trial
LICENSE=trial
#LICENSE=trial

# Port to expose Elasticsearch HTTP API to the host
ES_PORT=9200

# Port to expose Kibana to the host
KIBANA_PORT=5601

# Increase or decrease based on the available host memory (in bytes)
ES_MEM_LIMIT=3073741824
KB_MEM_LIMIT=2073741824

# SAMPLE Predefined Key only to be used in POC environments
ENCRYPTION_KEY=c34d38b3a14956121ff2170e5030b471551370178f43e5626eec58b04a30fae2

KEYCLOAK_ES_REDIRECT_URI=http://0.0.0.0:5601/api/security/oidc/callback
KEYCLOAK_ES_CLIENT_ID=kibana
KEYCLOAK_ES_CLIENT_SECRET=somesecret
KEYCLOAK_ES_AUTHORIZATION_ENDPOINT=https://keycloak/auth/realms/elastic/protocol/openid-connect/auth
KEYCLOAK_ES_TOKEN_ENDPOINT=https://keycloak/auth/realms/elastic/protocol/openid-connect/token
KEYCLOAK_ES_USER_INFO_ENDPOINT=https://keycloak/auth/realms/elastic/protocol/openid-connect/userinfo
KEYCLOAK_LOGOUT_API=https://keycloak/auth/realms/elastic/protocol/openid-connect/logout
KEYCLOAK_POST_LOGOUT_REDIRECT=http://0.0.0.0:5601/security/logged_out
KEYCLOAK_ES_JWKSET_PATH=https://keycloak/auth/realms/elastic/protocol/openid-connect/certs
KEYCLOAK_ES_ISSUER=https://keycloak/auth/realms/elastic


